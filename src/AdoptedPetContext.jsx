import { createContext } from "react";

// typescript will for you to have a default value, but right now we don't need one
const AdoptedPetContext = createContext();

export default AdoptedPetContext;